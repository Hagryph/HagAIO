# Packaged builds (tools/package.ps1 writes dist/HagAIO-v<version>.zip)
*.zip
dist/

# Scratch space (dumps, captures) — never tracked, excluded from deploy
Dev/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.bak
*.swp

# Local-only Claude settings
.claude/settings.local.json

# generated icon previews (the .tga in Media/ is the real asset)
tools/*.png
